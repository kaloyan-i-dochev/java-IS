package ID3;

/**
 * Represents a node in the ID3 decision tree.
 * This is an abstract class that serves as a base for both DecisionNode and LeafNode.
 */
public abstract class Node {
    // This class can be extended with common functionalities if needed.
}
